
  # High Fidelity Design

  This is a code bundle for High Fidelity Design. The original project is available at https://www.figma.com/design/foI0rnc029E5G7l1KUDUQ9/High-Fidelity-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  